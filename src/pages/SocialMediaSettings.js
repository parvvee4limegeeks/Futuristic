import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class SocialMediaSettings extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                Social Media Settings
            </div>
        )
    }
}
